switch..case คำสั่งทำหลายทางเลือก

switch (ค่าตั้งต้น){
  case ค่าตัวเเปร1: ชุดคำสั่ง;
                  break;
  case ค่าตัวเเปร2: ชุดคำสั่ง;
                  break;
  case ค่าตัวเเปร3: ชุดคำสั่ง;
                  break;
  defalt: ชุดคำสั่ง
}
void setup() {  // put your setup code here, to run onc
  int x = 3
  switch(x){
 ;          
  }
}

void loop() {
  switch(x){
    case 1: digitalWrite(LED!,HIGHT);
            break;
    case 1: digitalWrite(LED!,HIGHT);
            break;
    case 2: digitalWrite(LED!,HIGHT);
            break;
    case 3: digitalWrite(LED!,HIGHT);
            break;
    case 4: digitalWrite(LED!,HIGHT);
            break;
    case 5: digitalWrite(LED!,HIGHT);
            break;
    default:
            digitalWrite(LED1,LOW);
            digitalWrite(LED2,LOW);    
            digitalWrite(LED3,LOW);    
            digitalWrite(LED4,LOW);    
            digitalWrite(LEDe,LOW);          
  }
}

}
